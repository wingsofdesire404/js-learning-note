const sandwich = {
	bread: "dutch crunch",
	meat: "tuna",
	cheese: "swiss",
	toppings: ["lettuce", "tomato", "mustard"]
};

const { bread, meat } = sandwich; // pull out from sandwich and create new variables

console.log(bread, meat);
